# Projeto---Jornal
instalação nodejs:
  winget install OpenJS.NodeJS
  fechar o terminal.
instalação scss:
    npm install -g sass
    npm install -g npm@12.0.1
Atualizando automaticamente a cada alteração de scss 
    sass --watch style.scss:style.css
